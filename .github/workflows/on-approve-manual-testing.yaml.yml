name: Deploy tag to production server

on:
    workflow_dispatch:
        inputs:
            release_tag:
                description: 'Tag to deploy (e.g., v1.0.0)'
                required: true
                type: string
            release_id:
                description: 'Release ID from database'
                required: true
                type: string

env:
    SERVICE_NAME: ${{ github.event.repository.name }}
    SYSTEM_REPO: loom-system


jobs:
    deploy-to-production:
        runs-on: self-hosted

        steps:
            -   name: Validate inputs
                run: |
                    if [[ ! "${{ github.event.inputs.release_tag }}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+.*$ ]]; then
                      echo "❌ Неверный формат тега: ${{ github.event.inputs.release_tag }}"
                      echo "   Ожидается формат: v1.0.0"
                      exit 1
                    fi
                    
                    if [[ ! "${{ github.event.inputs.release_id }}" =~ ^[0-9]+$ ]]; then
                      echo "❌ Неверный формат release_id: ${{ github.event.inputs.release_id }}"
                      echo "   Ожидается числовое значение"
                      exit 1
                    fi
                    
                    echo "RELEASE_VERSION=${{ github.event.inputs.release_tag }}" >> $GITHUB_ENV
                    echo "RELEASE_ID=${{ github.event.inputs.release_id }}" >> $GITHUB_ENV
                    echo "✅ Входные параметры валидны"
                    echo "✅ Тег: ${{ github.event.inputs.release_tag }}"
                    echo "✅ Release ID: ${{ github.event.inputs.release_id }}"

            -   name: Load server configuration
                run: |
                    if [ ! -f "/root/.env.runner" ]; then
                      echo "❌ Файл конфигурации серверов .env.runner не найден!"
                      exit 1
                    fi
                    
                    # Загружаем переменные окружения из .env.runner
                    set -a
                    source /root/.env.runner
                    set +a
                    
                    # Проверяем наличие API конфигурации
                    if [ -z "$STAGE_DOMAIN" ] || [ -z "$PROD_DOMAIN" ]; then
                      echo "❌ API конфигурация не найдена в .env.runner!"
                      echo " Убедитесь, что есть переменные: STAGE_DOMAIN и PROD_DOMAIN"
                      exit 1
                    fi

                    if [ -z "$STAGE_HOST" ] || [ -z "$STAGE_PASSWORD" ]; then
                      echo "❌ Конфигурация stage сервера не найдена!"
                      echo "   Убедитесь, что в .env.runner есть переменные: STAGE_HOST и STAGE_PASSWORD"
                      exit 1
                    fi
                    
                    SERVICE_PREFIX=$(echo "${{ env.SERVICE_NAME }}" | tr '[:lower:]' '[:upper:]' | tr '-' '_')
                    SERVICE_PREFIX_VAR_NAME="${SERVICE_PREFIX}_PREFIX"
                    SERVICE_PREFIX="${!SERVICE_PREFIX_VAR_NAME}"
                    
                    echo "SERVICE_PREFIX=$SERVICE_PREFIX" >> $GITHUB_ENV
    
                    # Экспортируем все необходимые переменные
                    echo "PROD_HOST=$PROD_HOST" >> $GITHUB_ENV
                    echo "PROD_PASSWORD=$PROD_PASSWORD" >> $GITHUB_ENV
                    echo "PROD_DOMAIN=$PROD_DOMAIN" >> $GITHUB_ENV
                    
                    echo "LOOM_RELEASE_TG_BOT_PREFIX=$LOOM_RELEASE_TG_BOT_PREFIX" >> $GITHUB_ENV
                    echo "LOOM_INTERSERVER_SECRET_KEY=$LOOM_INTERSERVER_SECRET_KEY" >> $GITHUB_ENV

            -   name: Update status to production deployment
                run: |
                    RESPONSE=$(curl -s -w "\n%{http_code}" -X PATCH \
                      -H "Content-Type: application/json" \
                      -d '{
                        "release_id": ${{ env.RELEASE_ID }},
                        "status": "deploying",
                        "github_run_id": "${{ github.run_id }}",
                        "github_action_link": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                      }' \
                      "${{ env.PROD_DOMAIN }}${{ env.LOOM_RELEASE_TG_BOT_PREFIX }}/release")
                    
                    HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
                    BODY=$(echo "$RESPONSE" | head -n -1)
                    
                    if [ "$HTTP_CODE" -ne 200 ]; then
                      echo "❌ Не удалось обновить статус релиза. HTTP код: $HTTP_CODE"
                      echo "Ответ: $BODY"
                      exit 1
                    fi
                    
                    echo "✅ Статус обновлен на 'deploying'"

            -   name: Deploy to production server
                id: deploy_step
                run: |
                    SSH_OUTPUT=$(sshpass -p "${{ env.PROD_PASSWORD }}" ssh -o StrictHostKeyChecking=no root@${{ env.PROD_HOST }} -p 22 << 'EOF' 2>&1
                    set -e
                    
                    # Создаем директорию для логов если её нет
                    mkdir -p /var/log/deployments/production/${{ env.SERVICE_NAME }}
                    
                    # Создаем файл лога с именем тега для продакшн деплоя
                    LOG_FILE="/var/log/deployments/production/${{ env.SERVICE_NAME }}/${{ env.RELEASE_VERSION }}-prod.log"
                    
                    # Функция для логирования
                    log_message() {
                        local message="$1"
                        echo "$(date '+%Y-%m-%d %H:%M:%S') - $message" | tee -a "$LOG_FILE"
                    }
                    
                    log_message "🚀 Начинаем продакшн деплой тега ${{ env.RELEASE_VERSION }}"
                    log_message "🔗 Release ID: ${{ env.RELEASE_ID }}"
                    
                    cd loom/${{ env.SERVICE_NAME }}
                    
                    log_message "📥 Обновляем репозиторий и теги"
                    
                    # Сохраняем текущую ветку/тег для проверки
                    PREVIOUS_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "")
                    if [ -n "$PREVIOUS_TAG" ]; then
                        log_message "💾 Сохранен предыдущий тег для отката: $PREVIOUS_TAG"
                        echo "PREVIOUS_TAG=$PREVIOUS_TAG" > /tmp/previous_tag_${{ env.SERVICE_NAME }}.txt
                    else
                        log_message "⚠️ Предыдущий тег не найден, откат будет невозможен"
                    fi
                    
                    CURRENT_REF=$(git symbolic-ref --short HEAD 2>/dev/null || git describe --tags --exact-match 2>/dev/null || git rev-parse --short HEAD)
                    log_message "🔍 Текущее состояние: $CURRENT_REF"
                    
                    if git tag -l | grep -q "^${{ env.RELEASE_VERSION }}$"; then
                        log_message "🏷️ Локальный тег ${{ env.RELEASE_VERSION }} уже существует, удаляем для обновления"
                        git tag -d ${{ env.RELEASE_VERSION }} 2>&1 | tee -a "$LOG_FILE"
                    fi
                    
                    log_message "📥 Получаем обновления из удаленного репозитория"
                    git fetch origin 2>&1 | tee -a "$LOG_FILE"
                    
                    log_message "📥 Принудительно обновляем теги"
                    git fetch origin --tags --force 2>&1 | tee -a "$LOG_FILE"
                    
                    # Проверяем, что тег теперь доступен
                    if ! git tag -l | grep -q "^${{ env.RELEASE_VERSION }}$"; then
                        log_message "❌ Тег ${{ env.RELEASE_VERSION }} не найден в репозитории после обновления!"
                        log_message "📋 Доступные теги:"
                        git tag -l | tail -10 | tee -a "$LOG_FILE"
                        exit 1
                    fi
                    
                    log_message "✅ Тег ${{ env.RELEASE_VERSION }} найден и готов к использованию"

                    # Переключаемся на тег
                    log_message "🏷️ Переключаемся на тег ${{ env.RELEASE_VERSION }}"
                    git checkout ${{ env.RELEASE_VERSION }} 2>&1 | tee -a "$LOG_FILE"
                    
                    # Очищаем старые ветки (кроме main/master)
                    log_message "🧹 Очищаем старые ветки"
                    git for-each-ref --format='%(refname:short)' refs/heads | grep -v -E "^(main|master)$" | xargs -r git branch -D 2>&1 | tee -a "$LOG_FILE"
                    
                    # Очищаем удаленные ветки
                    log_message "🧹 Очищаем удаленные ветки"
                    git remote prune origin 2>&1 | tee -a "$LOG_FILE"
                    
                    log_message "✅ Переключение на тег ${{ env.RELEASE_VERSION }} завершено"
                    
                    log_message "🗄️ Запускаем миграции базы данных..."
                    # Запускаем миграции в отдельном контейнере
                    export $(cat env/.env.app env/.env.db env/.env.monitoring | xargs)
                    docker run --rm \
                      --network net \
                      -v ./:/app \
                      -w /app \
                      --env-file ../${{ env.SYSTEM_REPO }}/env/.env.app \
                      --env-file ../${{ env.SYSTEM_REPO }}/env/.env.db \
                      --env-file ../${{ env.SYSTEM_REPO }}/env/.env.monitoring \
                      python:3.11-slim \
                      bash -c '
                        echo "📦 Установка зависимостей..."
                        cd .github && pip install -r requirements.txt && cd ..
                        
                        echo "✅ Зависимости установлены"
                        echo "🚀 Запуск миграции..."
                        
                        python internal/migration/run.py prod --command up
                      ' 2>&1 | tee -a "$LOG_FILE"
                    
                    MIGRATION_EXIT_CODE=$?
                    
                    if [ $MIGRATION_EXIT_CODE -ne 0 ]; then
                        log_message "❌ Миграции завершились с ошибкой"
                        exit 1
                    fi
                    
                    log_message "✅ Миграции успешно применены"
                    
                    cd ../${{ env.SYSTEM_REPO }}
                    
                    export $(cat env/.env.app env/.env.db env/.env.monitoring | xargs)
                    
                    log_message "🔨 Начинаем сборку контейнера для тега ${{ env.RELEASE_VERSION }} на ПРОДАКШН..."

                    docker compose -f ./docker-compose/app.yaml up -d --build ${{ env.SERVICE_NAME }} 2>&1 | tee -a "$LOG_FILE"
                    
                    # Показываем информацию о созданных образах
                    log_message "📋 Созданные образы:"
                    docker images | grep ${{ env.SERVICE_NAME }} | tee -a "$LOG_FILE"
                    
                    check_health() {
                        # Если есть HTTP endpoint
                        if curl -f -s -o /dev/null -w "%{http_code}" ${{ env.PROD_DOMAIN }}${{ env.SERVICE_PREFIX }}/health | grep -q "200"; then
                            return 0
                        else
                            return 1
                        fi
                    }
                    
                    MAX_ATTEMPTS=5
                    ATTEMPT=1
                    SUCCESS=false
                    
                    log_message "⏳ Ждем запуска сервиса на продакшн..."
                    sleep 15
                    
                    while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
                        log_message "🔍 Проверка health на продакшн (попытка $ATTEMPT из $MAX_ATTEMPTS)..."
                    
                        if check_health; then
                            log_message "✅ Health check на продакшн пройден!"
                            SUCCESS=true
                            break
                        else
                            log_message "⏳ Health check не пройден, ждем..."
                            sleep 20
                        fi
                    
                        ATTEMPT=$((ATTEMPT + 1))
                    done
                    
                    rollback_to_previous() {
                        log_message "🔄 Откатываемся на тег: $PREVIOUS_TAG"
                        
                        cd /root/loom/${{ env.SERVICE_NAME }}
                    
                        docker run --rm \
                          --network net \
                          -v ./:/app \
                          -w /app \
                          -e PREVIOUS_TAG="$PREVIOUS_TAG" \
                          --env-file ../${{ env.SYSTEM_REPO }}/env/.env.app \
                          --env-file ../${{ env.SYSTEM_REPO }}/env/.env.db \
                          --env-file ../${{ env.SYSTEM_REPO }}/env/.env.monitoring \
                          python:3.11-slim \
                          bash -c '
                            echo "📦 Установка зависимостей..."
                            cd .github && pip install -r requirements.txt && cd ..
                            
                            echo "✅ Зависимости установлены"
                            echo "🚀 Запуск откачивания миграции..."
                            
                            python internal/migration/run.py prod --command down --version $PREVIOUS_TAG
                          ' 2>&1 | tee -a "$LOG_FILE"
                    
                        
                        log_message "🏷️ Переключаемся на предыдущий тег $PREVIOUS_TAG"
                        git checkout $PREVIOUS_TAG 2>&1 | tee -a "$LOG_FILE"
                    
                        cd /root/loom/${{ env.SYSTEM_REPO }}
                        
                        log_message "🔨 Пересобираем контейнер с предыдущим тегом..."
                        docker compose -f ./docker-compose/app.yaml up -d --build ${{ env.SERVICE_NAME }} 2>&1 | tee -a "$LOG_FILE"
                        
                        log_message "⏳ Ждем запуска сервиса после отката..."
                        sleep 15
                        
                        # Проверяем работоспособность после отката
                        ROLLBACK_ATTEMPTS=3
                        ROLLBACK_SUCCESS=false
                        
                        for i in $(seq 1 $ROLLBACK_ATTEMPTS); do
                            log_message "🔍 Проверка health после отката (попытка $i из $ROLLBACK_ATTEMPTS)..."
                            
                            if check_health; then
                                log_message "✅ Health check после отката пройден!"
                                ROLLBACK_SUCCESS=true
                                break
                            else
                                log_message "⏳ Ждем..."
                                sleep 10
                            fi
                        done
                        
                        if [ "$ROLLBACK_SUCCESS" = true ]; then
                            log_message "✅ Откат на $PREVIOUS_TAG выполнен успешно"
                            return 0
                        else
                            log_message "❌ Откат выполнен, но health check не прошел"
                            log_message "📋 Логи контейнера после отката:"
                            docker logs --tail 100 ${{ env.SERVICE_NAME }} 2>&1 | tee -a "$LOG_FILE"
                            return 1
                        fi
                    }
                    
                    if [ "$SUCCESS" = false ]; then
                        log_message "❌ Health check не пройден после $MAX_ATTEMPTS попыток"
                        log_message "📋 Логи контейнера:"
                        docker logs --tail 100 ${{ env.SERVICE_NAME }} 2>&1 | tee -a "$LOG_FILE"
                        
                        # Выполняем откат
                        log_message "🔄 Запускаем процедуру отката..."
                        if rollback_to_previous; then
                            log_message "✅ Откат выполнен успешно, сервис работает на предыдущей версии"
                            log_message "⚠️ Деплой тега ${{ env.RELEASE_VERSION }} отменен из-за неудачного health check"
                            exit 1
                        else
                            log_message "❌ Откат не удался или сервис не работает после отката"
                            log_message "🚨 КРИТИЧЕСКАЯ ОШИБКА: требуется ручное вмешательство"
                            exit 1
                        fi
                    fi
                    
                    log_message "🎉 Тег ${{ env.RELEASE_VERSION }} успешно развернут на ПРОДАКШН и приложение работает!"
                    log_message "📁 Лог продакшн деплоя сохранен в: $LOG_FILE"
                    
                    # Выводим последние строки лога для GitHub Actions
                    echo "📋 Последние строки лога продакшн деплоя:"
                    tail -20 "$LOG_FILE"
                    EOF
                    )
                    
                    SSH_EXIT_CODE=$?
                    
                    if [ $SSH_EXIT_CODE -ne 0 ]; then
                        echo "❌ Ошибка при деплое на продакшн сервер:"
                        echo "$SSH_OUTPUT"
                        exit 1
                    fi
                    
                    echo "✅ Деплой на продакшн завершен успешно"

            -   name: Update status on failure
                if: failure()
                run: |
                    curl -s -X PATCH \
                      -H "Content-Type: application/json" \
                      -d '{
                        "release_id": ${{ env.RELEASE_ID }},
                        "status": "production_failed"
                      }' \
                      "${{ env.PROD_DOMAIN }}${{ env.LOOM_RELEASE_TG_BOT_PREFIX }}/release"
                    echo "✅ Статус обновлен на 'production_failed'"
                    echo "📊 Сервер: ${{ env.SERVER_HOST }}"
                    echo "🏷️ Версия: ${{ env.RELEASE_VERSION }}"
                    echo "🔗 Release ID: ${{ env.RELEASE_ID }}"
                    echo "📁 Лог продакшн деплоя: /var/log/deployments/production/${{ env.SERVICE_NAME }}/${{ env.RELEASE_VERSION }}-prod.log"

            -   name: Update status to production success
                if: success()
                run: |
                    curl -s -X PATCH \
                      -H "Content-Type: application/json" \
                      -d '{
                        "release_id": ${{ env.RELEASE_ID }},
                        "status": "deployed"
                      }' \
                      "${{ env.PROD_DOMAIN }}${{ env.LOOM_RELEASE_TG_BOT_PREFIX }}/release"
                    echo "✅ Статус обновлен на 'deployed'"

            -   name: Verify production deployment
                if: success()
                run: |
                    echo "🎉🎉🎉 ПРОДАКШН ДЕПЛОЙ ЗАВЕРШЕН УСПЕШНО 🎉🎉🎉"
                    echo "📊 Сервер: ${{ env.SERVER_HOST }}"
                    echo "🏷️ Версия: ${{ env.RELEASE_VERSION }}"
                    echo "✅ Статус: Успешно развернуто на продакшн"
                    echo "🔗 Release ID: ${{ env.RELEASE_ID }}"
                    echo "📁 Лог продакшн деплоя: /var/log/deployments/production/${{ env.SERVICE_NAME }}/${{ env.RELEASE_VERSION }}-prod.log"